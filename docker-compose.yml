services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: riigikogu-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./database:/app/database
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    deploy:
      resources:
        limits:
          memory: 1024M

  # Cloudflare Tunnel (valikuline: kui soovid tunnelit otse dockeris jooksutada)
  # tunnel:
  #   image: cloudflare/cloudflared:latest
  #   container_name: riigikogu-tunnel
  #   restart: unless-stopped
  #   command: tunnel --no-autoupdate run --token ${CLOUDFLARE_TUNNEL_TOKEN}
  #   depends_on:
  #     - backend
